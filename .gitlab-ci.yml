stages:
    - deploy

deploy:
    stage: deploy
    script:
        - python3 -m pip3 install -r requirements.txt
        - python3 main.py --log info
    environment:
        name: prod
        url: prod.toilet.apap04.com
    only:
        - master